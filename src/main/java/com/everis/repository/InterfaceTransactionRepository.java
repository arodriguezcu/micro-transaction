package com.everis.repository;

import com.everis.model.Transaction;

/**
 * Interface de Metodos del Repositorio.
 */
public interface InterfaceTransactionRepository extends InterfaceRepository<Transaction, String> {
  
}
